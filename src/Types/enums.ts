export enum FieldType {
  TEXT = 0,
  MEDIA = 1,
  NUMBER = 2,
  DATE = 3,
  URL = 4,
  COLOR = 5,
  FORMATE_TEXT = 6,
}
export enum AccessToken {
  READ = 1,
  FULL = 0,
}

export enum STATUS{
  BAD_REQUEST = 400,
  UNAUTHORIZE = 401,
  NOT_FOUND =404,
  INTERNAL_SERVER_ERROR = 500,
  OK = 200,
  CREATED = 201
}
